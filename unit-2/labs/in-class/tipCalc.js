/*
    Make a tip calculator using a function
    Have it RETURN the value
    Capture that returned value in a VARIABLE
    Print that variable
*/


// tip calculator steps
// 1. make i

//* METHOD 1
/* function tipCalc(pretiptotal) {
    let fifteen = ((pretiptotal * .15) + pretiptotal)
    return fifteen
}

console.log(tipCalc(50)) */

// * METHOD 2
/* let tipped = function mealcost() {
    let fifteen = (pretipmeal * 1.15);
    let twenty = (pretipmeal * 1.2);
    return twenty;
}

let pretipmeal = 80;
console.log(tipped()); */

// * METHOD 3 FOUND ONLINE